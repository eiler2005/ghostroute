# Per-IP:port exceptions that should bypass VPN and use the normal WAN route.
# Format: <proto> <ipv4> <port>
#
# Example:
# tcp 203.0.113.10 22

# OpenClaw host: HTTPS should stay routable via VPN, but SSH works only direct.
tcp 203.0.113.10 22
