# Per-IP:port exceptions that should bypass VPN and use the normal WAN route.
# Format: <proto> <ipv4> <port>
#
# Example:
# tcp 203.0.113.10 22
#
# Personal production overrides should live in:
#   secrets/no-vpn-ip-ports.local.txt
# This file is ignored by git and appended during deploy when present.
