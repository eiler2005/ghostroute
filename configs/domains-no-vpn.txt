# Домены, которые НЕ нужно маршрутизировать через VPN.
# Один домен на строку. Комментарии начинаются с #.
# Поддомены учитываются автоматически: example.com покрывает www.example.com и т.д.
# После изменений запустите ./deploy.sh
#
# ПРИМЕЧАНИЕ: все российские TLD (.ru, .su, .рф, .москва, .tatar, .moscow)
# пропускаются автоматически в скрипте domain-auto-add.sh.
# Этот файл — только для российских сервисов на НЕроссийских доменах.

# --- Российские сервисы на зарубежных TLD ---
championat.com
meduza.io
