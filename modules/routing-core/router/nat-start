#!/bin/sh

# Channel A NAT is owned by stealth-route-init.sh from firewall-start.
# This hook intentionally has no legacy WireGuard policy-routing work after the
# WireGuard decommission cleanup.
exit 0
